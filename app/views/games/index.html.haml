.row
  .panel.panel-primary
    .panel-heading
      %h3 Games
    .panel-body
      %table.table
        %thead
          %tr.row
            %th Date/Time
            %th Home Team
            %th Visiting Team
            %th Score
        %tbody
          - @games.each do |game|
            %tr.row
              %td= link_to(game.game_time.strftime("%m/%d %H:%M"), game)
              %td= link_to(game.home_team.name, game.home_team)
              %td= link_to(game.visiting_team.name, game.visiting_team)
              %td
